# Cloudflare Pages config — guides.brandon-behring.dev (hub repo).
#
# Not yet deployed. The Pages project gets created from this repo at launch
# (roadmap v2 §5 Phase L0; runbook: docs/deploy-cloudflare-pages.md).
#
#   Framework preset: Astro
#   Build command:    npm run build
#   Build output:     dist
#   Root directory:   /
#   Node version:     22  (Astro 6 requires >=22.12.0)
#
# Phase L1 deliverable: Worker path proxy
#   guides.brandon-behring.dev/ai-engineering/* → guides-ai-engineering Pages project
# (pilot /experimentation/* proxy follows the same pattern when that guide ships).

name = "guides-hub"
compatibility_date = "2026-05-19"
pages_build_output_dir = "./dist"
